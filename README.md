## Document the yo, grunt, et cetera commands to get it setup here
mkdir slip-viewer-poc && cd $_
yo angular map
bower install angular-material angular-recursion polymer
# npm install -g grunt-cli
npm install grunt grunt-gh-pages
